import 'package:flutter/material.dart';

class Readable {
  var icon;
  var title;
  var content;
  MaterialColor color;

  Readable(
      {required this.icon,
      required this.title,
      required this.content,
      required this.color});
}
