import 'package:flutter/material.dart';

class musicPlayer extends StatelessWidget{
  const musicPlayer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: const Text('Planner')), body: );
  }
}